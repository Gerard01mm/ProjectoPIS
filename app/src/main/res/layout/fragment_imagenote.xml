<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/textViewImageNote"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    android:orientation="vertical"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="20dp"
    app:cardElevation="4dp"
    app:cardUseCompatPadding="true"
    tools:context=".Content.ImageNoteContentFragment">


    <LinearLayout
        android:id="@+id/lin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FAD2D2"
        android:gravity="right"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/saveImageNoteContent"
            android:layout_width="44dp"
            android:layout_height="41dp"
            android:layout_gravity="right"
            android:src="@drawable/ic_baseline_save_24" />
    </LinearLayout>

    <ImageView
        android:id="@+id/addImage"
        android:layout_width="207dp"
        android:layout_height="195dp"
        android:layout_gravity="center"
        app:srcCompat="@drawable/add_image" />

    <EditText
        android:id="@+id/editTextImageNote"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:ems="10"
        android:gravity="top"
        android:hint="@string/textImageNote"
        android:inputType="text" />

</LinearLayout>